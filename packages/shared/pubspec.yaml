name: shared
description: Shared data models and API client for HRMS platform
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.0

dependencies:
  dio: ^5.7.0
  json_annotation: ^4.9.0

dev_dependencies:
  json_serializable: ^6.8.0
  build_runner: ^2.4.13
