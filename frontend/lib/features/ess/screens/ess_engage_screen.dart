import 'package:flutter/material.dart';

class EssEngageScreen extends StatelessWidget {
  const EssEngageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ESS Engage Screen'));
  }
}
