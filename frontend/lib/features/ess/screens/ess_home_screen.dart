import 'package:flutter/material.dart';

class EssHomeScreen extends StatelessWidget {
  const EssHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ESS Home Screen'));
  }
}
